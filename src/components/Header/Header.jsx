import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <div className="container">
        {/* Header içeriği buraya gelecek */}
      </div>
    </header>
  );
};

export default Header;
