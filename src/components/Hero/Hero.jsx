import './Hero.scss';

const Hero = () => {
  return (
    <section className="hero">
      <div className="container">
        {/* Hero içeriği buraya gelecek */}
      </div>
    </section>
  );
};

export default Hero;
